import 'package:flutter/material.dart';

class EpisodesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('this is the list of episodes'),
    );
  }
}
